{
  "name": "@codex-monitor/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/index.ts",
    "install-server-launchd": "tsx src/cli/install-server-launchd.ts",
    "snapshot": "tsx src/cli/snapshot.ts",
    "install-launchd": "tsx src/cli/install-launchd.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@codex-monitor/shared": "workspace:*",
    "@fastify/cors": "^11.1.0",
    "@fastify/static": "^8.3.0",
    "fastify": "^5.6.1",
    "smol-toml": "^1.4.2"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
